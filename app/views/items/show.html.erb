<div class="w-full ">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <%= render @item %>

  
    <%= link_to "Torna Indietro", items_path, class: "mt-2 rounded-lg py-3 px-5 bg-gray inline-block font-medium" %>
  </div>
</div>
